services:
  - type: web
    name: couple-chat
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python chat_server.py"
    envVars:
      - key: HOST
        value: 0.0.0.0
